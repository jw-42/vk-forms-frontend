import { Group, Header, Placeholder } from "@vkontakte/vkui";

export function Templates() {
  return(
    <Group header={<Header>Шаблоны</Header>}>
      <Placeholder>
        Здесь пока ничего нет.
      </Placeholder>
    </Group>
  );
}