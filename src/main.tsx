import React from "react";
import ReactDOM from "react-dom/client";
import { AppConfig } from "./app/AppCinfig";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <AppConfig/>
  </React.StrictMode>
);
